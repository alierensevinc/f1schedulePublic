import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.alierensevinc.f1schedule',
    appName: 'f1schedule',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
